POSTGRES_USER=estoque_user
POSTGRES_PASSWORD=trocar_senha_forte
POSTGRES_DB=estoque

# DomÃ­nio para proxy reverso (Caddy/nginx), se usado
DOMAIN=seu-dominio.com
ACME_EMAIL=infra@seu-dominio.com

JWT_SECRET=trocar_chave_jwt_muito_forte

# CORS: origens permitidas no backend (separar por vÃ­rgula)
# Exemplos:
#   http://192.168.1.100      (frontend no IP local)
#   http://meudominio.com     (frontend por domÃ­nio)
#   *                         (qualquer origem â€” menos seguro)
CORS_ORIGIN=http://SEU_IP_FRONTEND

ALLOW_REGISTRATION=false

ADMIN_EMAIL=admin@seu-dominio.com
ADMIN_PASSWORD=trocar_senha_admin
POSTGRES_USER=estoque_user
POSTGRES_PASSWORD=trocar_senha_forte
POSTGRES_DB=estoque

# DomÃ­nio para proxy reverso (Caddy/nginx), se usado
DOMAIN=seu-dominio.com
ACME_EMAIL=infra@seu-dominio.com

JWT_SECRET=trocar_chave_jwt_muito_forte

# CORS: origens permitidas no backend (separar por vÃ­rgula)
# Exemplos:
#   http://192.168.1.100      (frontend no IP local)
#   http://meudominio.com     (frontend por domÃ­nio)
#   *                         (qualquer origem â€” menos seguro)
CORS_ORIGIN=http://SEU_IP_FRONTEND

ALLOW_REGISTRATION=false

ADMIN_EMAIL=admin@seu-dominio.com
ADMIN_PASSWORD=trocar_senha_admin
ADMIN_NOME=Administrador

# Portas expostas (opcional)
BACKEND_PORT=3000
FRONTEND_PORT=80

# SMTP Configuracoes (Reset de Senha)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-senha-de-app-gmail
FRONTEND_URL=https://seu-dominio.com
